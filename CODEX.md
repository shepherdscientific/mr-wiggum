# Codex Instructions

You are the implementation engine.
1. Read `prd.json` to find the current `passes: false` story.
2. Implement the story using your file-editing tools.
3. Run the relevant tests or validation for what you changed.
4. Update `prd.json` and **APPEND** to `progress.txt` within this same session.
5. Commit the completed story before finishing the iteration.
6. If all PRD items are done, output `<promise>COMPLETE</promise>`.
