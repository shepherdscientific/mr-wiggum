# Ralph working files (generated during runs)
prd.json
progress.txt
.last-branch
.ralph-cost.log
.ralph-metrics.jsonl

# Real env files (secrets) — never commit. Templates (*.example) ARE committed.
.env
.env.*
!*.example

# Archive is optional to commit
# archive/

# OS files
.DS_Store

#Claude
.claude/
